import add from "../../Utils/calculate";


const Sunglass = () => {
    
    const first = 20;
    const second = 20;
    const sum = add(first, second)
    console.log(sum);


    return (
        <div>
            
        </div>
    );
};

export default Sunglass;